# SkipBar
A lightweight browser extension designed for mouse only HTPC (Home Theater PC) setups. Nudge adds a simple, auto-hiding quick-skip toolbar to the top of the screen when watching fullscreen video.
